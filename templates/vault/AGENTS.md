# LLM Wiki Schema (Codex)

이 볼트의 운영 규칙과 워크플로우 정본은 [`CLAUDE.md`](CLAUDE.md)다. Codex도 그 규칙을 그대로
따른다. 기계 검사 스키마 계약은 [`_meta/schema.md`](_meta/schema.md)에 있으며 `llmwiki vault lint`가
검사한다.

작업 유형(Ingest / Query Filing / Lint / Reflect)과 index/log 규칙, Git 운영 규칙은 모두
`CLAUDE.md`를 정본으로 삼는다. 여기서 재기술하지 않는다.
